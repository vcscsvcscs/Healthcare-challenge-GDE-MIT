name: eva_healthcare_assistant
description: "Eva Health Assistant - Voice-first health data aggregation for Hungarian users."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  provider: ^6.1.2
  shared_preferences: ^2.3.4
  flutter_local_notifications: ^18.0.1
  fl_chart: ^0.70.2
  intl: ^0.20.2
  path_provider: ^2.1.5
  permission_handler: ^11.3.1
  timezone: ^0.10.0
  flutter_timezone: ^3.0.1
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
